Сверстай секцию header (используй семантический тег) внутрь ставь div с класоом container

Внутри блока с класоом container сделай верстку по следующему описанию:
    - вставь блок обертку
    - внктри блока обертки размести 2 блока:
        - в левый размести логотип (в теге img) обернутый в ссылку картирку возьми public\img\logo.svg
        - В правом размести блок с меню внутрь которого размести тег nav в котором расположи меню в виде ul списка в нем должны быть следующие пункты:Концепция, Прайс, О компании, Контакты (с ul списку не применяй стили он будет выводиться из админки)

При верстке используй БЭМ натацию

Стилизуй сверстанный блок начиная с блока с классом header__wrapper согласно следующему описанию:
    - блок header__wrapper flex; flrx-wrap:wrap; gap:20px;
    - логотип внутри header__left должен быть шириной 150 px
    - Блок header__right должен быть flex; align-item: end;
    - Блок header__right должен иметь нижнее подчеркивание в 1px сплошной линией цвет $main_color
    - блок header__nav должен быть отцентрован по горизонтали

___________


Сверстанный блок с класоом concept согласно следующему описанию:
    - Внутри блока concept два блока левый и правый
    - в левом блоке есть еще 3 вложенных блока
        - обертка для фото с фото которое нужно взять public\img\concept\foto_1.webp
        - Блок с текстом
        - обертка для фото с фото которое нужно взять public\img\concept\foto_4.webp
    - в левом блоке есть еще 2 вложенных блока
        - Блок с текстом
        - обертка в которой еще 2 блока обертки с фото которое нужно взять public\img\concept\foto_2.webp и которое нужно взять public\img\concept\foto_3.webp соответственно
        - обертка в которой размещены блок с текстом  и обертка для фото с фото которое нужно взять public\img\concept\foto_5.webp

При верстке используй БЭМ натацию
____________

В  файле миграций database\migrations\2025_09_26_162942_create_prices_table.php обяви поля таблицы руководствуясь списком:
- Раздел (строка)
- Подраздел (строка, nullable)
- Описание (Текст, nullable)
- Позиции (json, nullable)

Обязательно проставь ->comment
Так же на основе этой миграции заполни поле $fillable, $casts соответствующей модели
________

Для ModelResource админ панели moonshine который находится в файле app\MoonShine\Resources\PriceResource.php проставь поля опираясь на миграцию database\migrations\2025_09_26_162942_create_prices_table.php.

В класс ресурса добавь следующие поля:

protected string $title = 'Прайс';
protected string $column = 'section';

Если поля существуют замени в них содержимое.

В метод indexFields вынеси поля:

'section'
'subsection'

В методах formFields и detailFields распиши все поля кроме created_at и updated_at

При формировании полей используй ->comment в миграции в качестве имени поля. Пример: Text::make("Заголовок", 'title'),
Для полей миграции с именем img или image, cover, map,  используй компонент Image так же добавляй ->dir(''), строку внутри оставляй пустой.  Пример: Image::make('Изображение', 'img')->dir(''),
Для полей миграции с именем slug используй компонент Slug.  Пример: Slug::make("Ссылка", 'slug'),
Для полей миграции с типом boolean используй компонент Switcher.  Пример: Switcher::make('По умолчанию', 'is_default'),
Для полей миграции с именем description используй компонент TinyMce.  Пример: TinyMce::make('Описание', 'description'),
Для полей миграции с типом json используй компонент Json, с модификаторм ->fields в котором по умолчанию должно быть поле Position::make().  Пример:
Json::make('Галерея', 'images')->fields([
    Position::make(),
]),

Так же ко всем полям json добавляй ->removable()

Так же опираясь на миграцию заполни метод rules, при этом не включай поля которые в миграции помеченны nullable(), так же не включай поле slug

Так же добавь метод filters по следующему шаблону

    protected function filters(): iterable
    {
        return [

        ];
    }

В этом методе должны быть следующие поля:
'section'
'subsection'
